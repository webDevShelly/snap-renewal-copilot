# Required. The agent runs on the OpenAI Agents SDK.
OPENAI_API_KEY=sk-...

# Optional. Defaults to the Agents SDK's default model when unset.
# OPENAI_DEFAULT_MODEL=gpt-5.6-luna

# Optional. When all three are set, texts go out over real SMS instead of the console/web thread.
# Point your Twilio number's inbound messaging webhook at POST /api/sms/inbound.
# TWILIO_ACCOUNT_SID=AC...
# TWILIO_AUTH_TOKEN=...
# TWILIO_FROM_NUMBER=+1...

# Optional. Move the knowledge base somewhere else (defaults to ./data).
# DATA_DIR=./data
